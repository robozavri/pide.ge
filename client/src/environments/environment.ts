export const environment = {
  production: false,
  apiUrl: `http://${location.hostname}:4002`,
  host: `http://${location.hostname}:4001`,
  resourceNameSpace: `http://${location.hostname}:4002`,
  hmr: false,
};